name: Build binaries for release

on:
  push:
    branches:
      - main # 新增：當有推送到 'main' 分支時，也觸發工作流程
    tags:
      - 'v*.*.*'
      - 'latest' # 當推送 'latest' 標籤時觸發

jobs:
  build:
    name: Build ${{ matrix.platform.name }}
    runs-on: ${{ matrix.platform.os }}
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix:
        platform:
          # Windows
          - name: Windows x86_64
            os: windows-latest
            target: x86_64-pc-windows-msvc
            artifact_name: aoede-windows-x86_64.exe
            binary_name: aoede.exe

          # Linux
          - name: Linux x86_64
            os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            artifact_name: aoede-linux-x86_64
            binary_name: aoede

          # macOS (Universal Binary - 兩個架構都支持)
          - name: macOS Universal
            os: macos-latest
            target: x86_64-apple-darwin
            artifact_name: aoede-macos-universal
            binary_name: aoede
            build_universal: true

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.platform.build_universal && 'x86_64-apple-darwin,aarch64-apple-darwin' || matrix.platform.target }}

      - name: Install dependencies (Linux)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y libavahi-compat-libdnssd-dev pkg-config libasound2-dev

      - name: Install dependencies (macOS)
        if: runner.os == 'macOS'
        run: |
          brew install pkg-config opus cmake
          # 修正 macOS 交叉編譯時 pkg-config 找不到 Homebrew 路徑的問題
          # 將路徑指向標準 Homebrew lib/pkgconfig 並設定 OPUS_LIB_DIR
          HOMEBREW_PREFIX=$(brew --prefix)
          echo "PKG_CONFIG_PATH=$HOMEBREW_PREFIX/lib/pkgconfig" >> $GITHUB_ENV
          echo "OPUS_LIB_DIR=$HOMEBREW_PREFIX/lib" >> $GITHUB_ENV

      - name: Install dependencies (Windows)
        if: runner.os == 'Windows'
        run: |
          # 解決 dns-sd 依賴問題：由於 'bonjour-sdk' 套件已失效，我們改為禁用 pkg-config。
          # 強制禁用 pkg-config，讓 Rust 連結器使用系統路徑中的 dnssd.lib。
          echo "AVAHI_COMPAT_LIBDNS_SD_NO_PKG_CONFIG=1" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append

      - name: Cache cargo build
        uses: Swatinem/rust-cache@v2
        with:
          key: ${{ matrix.platform.target }}

      - name: Build release binary
        run: cargo build --release --target ${{ matrix.platform.target }}

      - name: Build ARM64 binary (macOS Universal)
        if: matrix.platform.build_universal
        run: cargo build --release --target aarch64-apple-darwin

      - name: Create Universal Binary (macOS)
        if: matrix.platform.build_universal
        run: |
          lipo -create \
            target/x86_64-apple-darwin/release/${{ matrix.platform.binary_name }} \
            target/aarch64-apple-darwin/release/${{ matrix.platform.binary_name }} \
            -output ${{ matrix.platform.artifact_name }}

      - name: Rename binary (Unix)
        if: runner.os != 'Windows' && !matrix.platform.build_universal
        run: |
          cp target/${{ matrix.platform.target }}/release/${{ matrix.platform.binary_name }} ${{ matrix.platform.artifact_name }}

      - name: Rename binary (Windows)
        if: runner.os == 'Windows'
        run: |
          copy target\${{ matrix.platform.target }}\release\${{ matrix.platform.binary_name }} ${{ matrix.platform.artifact_name }}

      - name: Upload binary to release
        uses: softprops/action-gh-release@v1
        with:
          files: ${{ matrix.platform.artifact_name }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  # 新增 Job：在所有構建成功後，將當前提交標記為 'latest'
  update_latest_tag:
    name: Update 'latest' tag to current commit
    runs-on: ubuntu-latest
    needs: build
    # 邏輯變更：當 'build' 成功且 (觸發於 'v' 開頭標籤 或 觸發於 'main' 分支) 時運行
    if: success() && (startsWith(github.ref, 'refs/tags/v') || github.ref == 'refs/heads/main')
    permissions:
      contents: write # 必須要有寫入權限才能刪除和創建標籤
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        # 獲取完整歷史以便進行 Git 操作

      - name: Configure Git
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "41898282+github-actions[bot]@users.noreply.github.com"

      - name: Retag and Push 'latest'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          # 1. 刪除遠端已存在的 'latest' 標籤 (如果存在)
          git push --delete origin latest || true 
          
          # 2. 創建一個新的本地 'latest' 標籤，指向當前提交 SHA
          git tag -f latest "${{ github.sha }}"
          
          # 3. 將新的 'latest' 標籤推送到遠端
          git push origin latest
